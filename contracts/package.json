{
  "name": "shadowswap-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Confidential ShadowSwap vault contracts built with the iExec Nox Hardhat plugin.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.10",
    "typescript": "^5.5.4"
  },
  "//": "Add the Nox packages once you scaffold from nox-hardhat-starter: @iexec-nox/hardhat-plugin and the Nox Solidity library. See docs/phase-0-recon.md."
}
